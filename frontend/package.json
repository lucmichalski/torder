{
  "name": "tor-spider",
  "version": "0.0.1",
  "private": true,
  "homepage": "http://51.210.37.251:3000",
  "devDependencies": {
    "gh-pages": "^3.0.0",
    "react-scripts": "^3.4.1",
    "prettier": "^2.0.5"
  },
  "dependencies": {
    "@appbaseio/reactivesearch": "^3.10.1",
    "react": "^16.13.1",
    "react-dom": "^16.13.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
